server:
  port: 1003

  servlet:
    context-path:

spring:
  application:
    name: thaca-notification

  profiles:
    active: core
    include: blocking,dev

  output:
    ansi:
      enabled: always
